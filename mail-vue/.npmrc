# 依赖树用平铺（hoisted）布局。
#
# 为什么写进文件而不是每次敲 --node-linker=hoisted：
# 现有 node_modules 就是平铺的，一旦某次 `pnpm install` 漏了这个参数，pnpm 会按默认
# 的 isolated 布局把整棵树重新物化一遍——在 Windows 上实测要数小时（逐包 rename+delete）。
# 固化成配置后，任何人任何时候跑 install 都不会踩到这个坑。
node-linker=hoisted
